interface:
  display_name: "Kilocode"
  short_description: "Repo-specific patterns and workflows for kilocode"
  default_prompt: "Use the kilocode repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true